<div class="pb-12 mb-6 sm:w-full md:w-full lg:w-1/3 xl:w-1/3 sm:pr-8">
  <article class="overflow-hidden">
    <% if post.cover_image.attached? %>
      <%= image_tag post.cover_image.variant(resize_to_limit: [300, 300]), class: 'h-full w-full cursor-pointer' %>
    <% end %>
    <div class="px-4 pt-4 bg-white">
      <h1 class="mb-1 font-extrabold tracking-wide sm:text-4xl lg:text-2xl hover:underline">
        <%= link_to post.title, post %>&nbsp<%= '[PREVIEW]' if post.draft? %>
      </h1>
      <p class="mb-2 text-gray-600 sm:text-lg lg:text-sm">
        <%= post.published_at.strftime('%b, %Y') %>
      <p>
      <p class="mb-4 text-gray-800 sm:text-2xl lg:text-base">
        <%= post.intro %><br>
        <%= link_to 'Keep reading >>', link_to_post(post), class: "hover:underline sm:text-2xl lg:text-sm font-bold uppercase text-indigo-700"%>
      </p>
    </div>
  </article>
</div>
