<%= link_to 'ana.rb', posts_path, method: :get,
    class: 'h1 block text-xl px-4'%>
<div class="hidden md:flex flex-col md:flex-row flex-1 md:items-center md:justify-end">
  <%= link_to 'About', about_path, method: :get, class: 'block py-3 md:py-0 px-4 uppercase tracking-wide text-sm' %>
  <% if current_user %>
    <%= link_to 'Logout', logout_path, method: :delete, class: 'block py-3 md:py-0 px-4 uppercase tracking-wide text-sm' %>
  <% end %>
</div>
